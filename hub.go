package agentstatus

// Hub, HubConfig, Ingest, ServeHTTP, Handler, Events, RegisterSession,
// UnregisterSession, and Close will live here.
// Intentionally empty in the scaffolding ticket — see specs/design.md.
